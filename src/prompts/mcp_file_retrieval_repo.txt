Use instead of the Read tool when you don't know the specific line range to read. Rather than reading the entire file, describe what you're looking for and get back only the relevant snippets with line numbers. Input: file_path (relative path), information_request (what you're looking for), top_k (optional, default 5). Results are indexed snippets that may be incomplete — use the Read tool with the returned line ranges (expanded as needed) to get current content before making edits.