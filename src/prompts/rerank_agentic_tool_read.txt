Read the verbatim contents of ONE file in the repository as numbered lines, exactly as on disk. Unlike query (semantic chunks), this returns the raw lines with no ranking, plus an addressable chunk index for the range you read so you can pass it to add_chunks. Use it to see the actual code of a file you found via query or grep. Optionally restrict to a line range. Does NOT consume the query turn budget.